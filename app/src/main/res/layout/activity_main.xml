<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#202C2C"
    android:theme="@style/Theme.MaterialComponents"
    tools:context=".MainActivity">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/calc_session"
        android:layout_width="263dp"
        android:layout_height="63dp"
        android:backgroundTint="#B2D1CD"
        android:text="@string/start_calc_session"
        app:backgroundTint="#C5C5C5"
        app:cornerRadius="15dp"
        app:icon="@android:drawable/ic_media_play"
        app:layout_constraintBottom_toTopOf="@+id/scroll_statistics"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:strokeColor="#D9D9D9"
        app:strokeWidth="3dp" />

    <ScrollView
        android:id="@+id/scroll_statistics"
        android:layout_width="0dp"
        android:layout_height="526dp"
        android:background="@drawable/scrollview_bg"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1.0">

        <LinearLayout
            android:id="@+id/tanks"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="25dp" />
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>